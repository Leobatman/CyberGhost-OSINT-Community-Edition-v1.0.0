# =============================================================================
# CYBERGHOST OSINT ULTIMATE - Kubernetes Secrets
# =============================================================================
# Este arquivo contém exemplos de secrets. Em produção, use ferramentas como
# Sealed Secrets, External Secrets, ou HashiCorp Vault.
# NUNCA commite secrets reais no git!

apiVersion: v1
kind: Secret
metadata:
  name: cyberghost-secrets
  namespace: cyberghost
type: Opaque
stringData:
  # Database
  db-user: Y3liZXJnaG9zdA==  # cyberghost (base64)
  db-password: <base64_encoded_db_password>
  
  # Redis
  redis-password: <base64_encoded_redis_password>
  
  # API Keys (exemplos - substitua pelos reais)
  shodan-key: <base64_encoded_shodan_key>
  virustotal-key: <base64_encoded_virustotal_key>
  github-token: <base64_encoded_github_token>
  
  # JWT Secret (para autenticação)
  jwt-secret: <base64_encoded_jwt_secret>
  
  # Encryption Key (para dados sensíveis)
  encryption-key: <base64_encoded_encryption_key>

---
apiVersion: v1
kind: Secret
metadata:
  name: cyberghost-tls
  namespace: cyberghost
type: kubernetes.io/tls
stringData:
  tls.crt: |
    -----BEGIN CERTIFICATE-----
    <place_your_certificate_here>
    -----END CERTIFICATE-----
  tls.key: |
    -----BEGIN RSA PRIVATE KEY-----
    <place_your_private_key_here>
    -----END RSA PRIVATE KEY-----

---
apiVersion: v1
kind: Secret
metadata:
  name: cyberghost-registry
  namespace: cyberghost
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "https://index.docker.io/v1/": {
          "username": "cyberghost",
          "password": "your-dockerhub-password",
          "email": "cyberghost@protonmail.com",
          "auth": "<base64_encoded_auth>"
        }
      }
    }